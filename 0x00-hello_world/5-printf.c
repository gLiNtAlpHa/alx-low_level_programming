#include <stdio.h>

/**
*main - to print (with proper grammar, but the outcome is a piece of art)
*return 0, if executed properly,else non-zero
**/
int main(void)
{
  printf("with proper grammar, but the outcome is a piece of art,\n");
  return (0);
}
